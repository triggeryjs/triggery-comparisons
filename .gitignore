node_modules/
dist/
.DS_Store
*.log
.vite/
.parcel-cache/
.turbo/
coverage/
.env
.env.local

# Note: `measure/reports/` IS committed — README links to the markdown tables
# directly, and the diff on `pnpm measure` makes drift visible in PRs.
